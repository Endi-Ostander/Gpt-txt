

===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\config.py =====

# config.py

MEMORY_PATH = "Users/Brazers/Desktop/Endi-IA/P v0.1-rb/data/memory.json"
KNOWLEDGE_PATH = "Users/Brazers/Desktop/Endi-IA/P v0.1-rb/data/knowledge_base.json"
LOG_PATH = "Users/Brazers/Desktop/Endi-IA/P v0.1-rb/data/logs/ai.log"
LOG_LEVEL = "DEBUG"


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\Endi-IA_v0.2_checklist_UPDATED.md =====

# ✅ Endi-IA v0.2 — Checklist (обновлён 2025-07-03)

---
## 🎯 Цель версии v0.2

Создание стабильного ядра ИИ Endi, способного:
- классифицировать и интерпретировать фразы
- запоминать факты и знания
- формировать цели и планы обучения
- задавать уточняющие вопросы при непонимании
- фильтровать команды и мусор
- вести журнал мыслей

---

## 🧱 Архитектурные компоненты

- `core/learner/trainer.py` — центральная точка обработки фраз
- `core/memory/memory.py` — долговременная память
- `core/learner/curiosity.py` — список непонятых фраз
- `core/learner/generator.py` — генерация ответов
- `core/thinker/planner.py` — формирование целей обучения
- `core/thinker/meta_journal.py` — журнал размышлений
- `core/common/utils.py` — вспомогательные утилиты
- `tools/console.py` — консольный интерфейс

---
## 🧠 Основные завершённые задачи

- [x] Обработка утверждений (STATEMENT)
- [x] Обработка вопросов (QUESTION)
- [x] Сохранение фактов в `memory.json`
- [x] Сохранение знаний в `knowledge_base.json`
- [x] Добавлен `SpellCorrector` (автоисправление)
- [x] Генерация ответов на утверждения
- [x] Генерация фраз типа “я не знаю” на вопросы
- [x] Классификация фраз (PhraseType)
- [x] Токенизация текста
- [x] Логика правил в `rules_engine`
- [x] Журнал событий `MetaJournal`
- [x] Сохранение `Curiosity` в `data/curiosity.json`
- [x] Восстановление `Curiosity` при запуске
- [x] Добавлен метод `get_logged_goals()` в `MetaJournal`
- [x] Цели обучения (`/plan`) теперь синхронизированы с непонятыми фразами
- [x] Журнал сохраняется построчно (через `literal_eval`)
- [x] Повторные цели не дублируются
- [x] Добавление цели из вопроса в `Trainer`

---

## 🛠️ Ещё предстоит сделать в v0.2

- [ ] Команды `/unknowns`, `/clear unknowns`, `/explain <тема>`
- [ ] Команда `/cleanup` для очистки мусора в памяти
- [ ] Проверка валидности утверждений (`is_valid_statement`)
- [ ] Автоматическое уточнение при неполной фразе (например, “кот” → уточняющий вопрос)
- [ ] Поддержка `/goal <цель>` — вручную задать цель
- [ ] Экспорт всех фактов (`/export`)
- [ ] Мини-тесты: `test_trainer`, `test_planner`, `test_generator`


---

## 🧠 Приоритеты (по убыванию важности)

1. Проверка структуры фраз (качество обучения)
2. Сохранение любознательности между сессиями
3. Расширение `/plan` и целей
4. Очистка памяти (мусорные утверждения)
5. Юнит-тесты для устойчивости
6. Расширение интерфейса (`/explain`, `/unknowns`)
7. Фильтрация журналов и экспорт

---

## 🔮 Прогноз на v0.3

Следующий этап сосредоточится на:
- Внедрении внешнего ИИ (GPT) в роли наставника
- Начале логических гипотез (модуль `hypothesis`)
- Сохранении и управлении любознательностью
- Подготовке к ingestion больших текстов (v0.4)
- Первых шагах API-интеграции

---

## 💡 Дополнительно

- Возможна реализация `/read` для чтения текстов (начиная с v0.4)
- Начало структуры внешних подключений (`external_ai.py`)
- Введение веса или значимости фактов

---
## 🧩 Примечания

- `/plan` теперь использует `MetaJournal.get_logged_goals()` и `Curiosity.get_questions()`
- Поведение `/plan` устойчиво к дублирующимся вопросам
- Уточняющие вопросы генерируются при нераспознанных вводах
- Журнал записывается в `meta_journal.json` по одной строке на запись



===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\memory_report.md =====

# 🧠 Память ИИ

## 📌 Факты

- **вода** — *состоит из* — водорода и кислорода
  Источник: `начальные знания`
  Время: `2025-06-25T12:00:00`

- **деревья** — *могут* — гореть
  Источник: `Деревья могут гореть`
  Время: `2025-06-25T18:18:49.854477`

- **Тест** — *есть* — данные
  Источник: `тест`
  Время: `2025-06-25T19:26:35.762239`

- **солнце** — *это* — звезда
  Источник: `Солнце это звезда`
  Время: `2025-06-25T19:58:12.328126+00:00`

- **"Солнце"** — *"Солнце* — - это звезда, которая является источником света и тепла для Земли." ["астрономия", "солнце",
  Источник: `"звезды"]`
  Время: `2025-06-25T20:23:55.058045+00:00`

- **тест** — *есть* — пример
  Источник: `None`
  Время: `2025-06-28T14:28:37.026743+00:00`

- **солнце** — *is* — это звезда
  Источник: `None`
  Время: `2025-06-30T19:30:09.964193+00:00`

- **блимбазоид** — *коллапсировал* — метасферу
  Источник: `блимбазоид коллапсировал метасферу`
  Время: `2025-06-30T20:07:54.574359+00:00`

- **блимбазоид** — *is* — коллапсировал метасферу
  Источник: `None`
  Время: `2025-06-30T20:07:54.578938+00:00`

- **plan** — *is* — 
  Источник: `None`
  Время: `2025-06-30T20:08:07.085070+00:00`

- **кот** — *это* — животное
  Источник: `кот это животное`
  Время: `2025-07-03T12:32:31.778092+00:00`

- **кот** — *is* — это животное
  Источник: `None`
  Время: `2025-07-03T12:32:31.856519+00:00`

- **слон** — *это* — животное
  Источник: `слон это животное`
  Время: `2025-07-03T12:35:34.397028+00:00`

- **слон** — *is* — это животное
  Источник: `None`
  Время: `2025-07-03T12:35:34.425334+00:00`

- **гримпфацианский** — *коллапс* — 
  Источник: `гримпфацианский коллапсон`
  Время: `2025-07-03T12:36:20.909891+00:00`

- **гримпфацианский** — *is* — коллапс
  Источник: `None`
  Время: `2025-07-03T12:36:21.217084+00:00`

- **кот** — *животное* — 
  Источник: `кот жв0тное`
  Время: `2025-07-03T12:38:15.034313+00:00`

- **кот** — *is* — животное
  Источник: `None`
  Время: `2025-07-03T12:38:15.049804+00:00`

- **энергия** — *это* — сила
  Источник: `энергия это сила`
  Время: `2025-07-03T12:56:25.363747+00:00`

- **энергия** — *is* — это сила
  Источник: `None`
  Время: `2025-07-03T12:56:25.372105+00:00`

## 📚 Знания



===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\README.md =====

---

````markdown
# 🤖 GPT-TXT: Обучаемый ИИ (v0.1)

Первая рабочая версия ИИ, способного обучаться, анализировать текст, сохранять знания и распознавать смысл фраз. Проект разрабатывается без использования внешних ИИ-сервисов (ChatGPT и т.п.) — полностью автономно и локально.

---

## 📌 Возможности (v0.1)

- 📊 Классификация фраз (вопросы, утверждения, команды, неизвестные)
- 🧠 Хранение и поиск фактов/знаний (в JSON)
- 🧹 Очистка и токенизация текста
- ✍️ Автоматическое исправление опечаток
- 📋 Правила извлечения знаний
- ❓ Любознательность: запись непонятных фраз
- 💾 Экспорт памяти (`.md`, `.csv`)
- 🧪 Покрытие `pytest`: 35 тестов, >93% охвата

---

## 🚀 Запуск

### 🧠 Запуск обучения:
```bash
python run.py
````

### 🛠 Расширенная консоль:

```bash
python tools/dev_console_ext.py
```

## 🗂 Структура проекта

```
project_root/
│
├── run.py                         # Основной файл запуска ИИ (консольное обучение)
├── config.py                      # Настройки (пока пустой, резерв под конфигурации)
│
├── core/                          # Вся логика ИИ
│   ├── common/                    # Общие утилиты и типы данных
│   │   ├── types.py               # Типы (фраз, знаний, фактов и т.п.)
│   │   └── utils.py               # Функции: ID, время, пути, JSON
│   │
│   ├── learner/                   # Модули обучения и обработки смысла
│   │   ├── trainer.py             # Основной обработчик входного текста
│   │   ├── rules.py               # Правила классификации фраз
│   │   ├── curiosity.py           # Хранение непонятных вопросов
│   │   └── reinforce.py           # Подкрепление знаний (вознаграждения)
│   │
│   ├── memory/                    # Память ИИ
│   │   └── memory.py              # Работа с фактами и знаниями (JSON)
│   │
│   ├── processor/                 # Обработка текста
│   │   ├── tokenizer.py           # Токенизация и очистка текста
│   │   ├── classifier.py          # Определение типа фразы
│   │   ├── parser.py              # Объединение токенизации и классификации
│   │   └── spellchecker.py        # Орфографические исправления
│   │
│   └── inputs/                    # Ввод внешней информации
│       ├── text_input.py          # Заглушка (будущий интерфейс)
│       └── html_scraper.py        # Получение текста с веб-страниц (BeautifulSoup)
│
├── tools/                         # Утилиты и консольные инструменты
│   └── dev_console_ext.py         # Расширенная консоль с командами (/help, /mem и т.д.)
│
├── data/                          # Память и знания
│   ├── memory.json                # Факты (subject → predicate → object)
│   └── knowledge_base.json        # Знания (title, content, tags, type)
│
├── tests/                         # Pytest-тесты для всех модулей
│   ├── test_tokenizer.py
│   ├── test_classifier.py
│   ├── test_memory.py
│   ├── test_rules.py
│   ├── test_spellchecker.py
│   ├── test_utils.py
│   ├── test_curiosity.py
│   ├── test_parser.py
│   ├── test_reinforce.py
│   ├── test_text_input.py
│   └── test_html_scraper.py
│
├── requirements.txt              # Зависимости проекта (pyspellchecker, pytest и др.)
└── README.md                     # Описание проекта, запуск, структура

---

## 📦 Установка

### 1. Установить зависимости:

```bash
pip install -r requirements.txt
```

### 2. Для тестов и покрытия:

```bash
pytest --cov=core tests/
```

---

## 📅 Планы на v0.2+

* 📤 Генерация ответов и гипотез
* 🌐 Автосбор знаний с сайтов
* 🧠 Морфология, лемматизация, синонимы
* 🔐 Сохранение поведения и обучения между сессиями

---

## 👨‍💻 Автор

Логика, архитектура, обучение и тестирование реализованы модульно.

---

## 📝 Лицензия

MIT (по умолчанию) — можно адаптировать под GPL или Creative Commons при желании.

```




===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\requirements.txt =====

pyspellchecker
pytest
beautifulsoup4


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\run.py =====

from core.learner.trainer import Trainer
import sys

# Для вывода можно импортировать отдельно функции из dev_console_ext, 
# но лучше вынести их в отдельный утилитный модуль, чтобы избежать циклов.

def main():
    trainer = Trainer()
    print("Введите текст для обучения ИИ (команды начинаются с /, exit — выход):")

    while True:
        user_input = input("> ").strip()
        if not user_input:
            continue

        if user_input.startswith("/"):
            parts = user_input[1:].split()
            cmd = parts[0].lower()

            if cmd == "exit":
                break
            elif cmd == "mem":
                if len(parts) == 1:
                    print("❗ Уточните: facts, knowledge или export")
                    continue
                subcmd = parts[1].lower()

                if subcmd == "facts":
                    subj = parts[2] if len(parts) > 2 else None
                    # Вывод фактов — здесь вызывай функцию из console или отдельно
                    print(f"Вывести факты для: {subj or 'все'} (реализуй логику отдельно)")
                elif subcmd == "knowledge":
                    tag = parts[2] if len(parts) > 2 else None
                    print(f"Вывести знания для тега: {tag or 'все'} (реализуй логику отдельно)")
                elif subcmd == "export":
                    if len(parts) < 3:
                        print("❗ Укажите формат: md или csv")
                    elif parts[2] == "md":
                        print("Экспорт в md (реализуй логику отдельно)")
                    elif parts[2] == "csv":
                        print("Экспорт в csv (реализуй логику отдельно)")
                    else:
                        print("❗ Неизвестный формат. Используйте md или csv.")
                else:
                    print(f"❗ Неизвестная команда: {subcmd}")
            else:
                print(f"❗ Неизвестная команда: /{cmd}")
        else:
            trainer.process_text(user_input)

if __name__ == "__main__":
    main()


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\проект.txt =====


## 🔧 Общая цель
Создать полностью обучаемого ИИ, способного:
- самостоятельно анализировать текст,
- извлекать факты и знания,
- запоминать новую информацию,
- задавать вопросы при непонимании (любознательность),
- обучаться на основе собственных наблюдений,
- расширять свою базу знаний без внешних ИИ.
## 🧱 Структура проекта
project_root/
│
├── run.py                         # Основной файл запуска ИИ (консольное обучение)
├── config.py                      # Настройки (пока пустой, резерв под конфигурации)
│
├── core/                          # Вся логика ИИ
│   ├── common/                    # Общие утилиты и типы данных
│   │   ├── types.py               # Типы (фраз, знаний, фактов и т.п.)
│   │   └── utils.py               # Функции: ID, время, пути, JSON
│   │
│   ├── learner/                   # Модули обучения и обработки смысла
│   │   ├── trainer.py             # Основной обработчик входного текста
│   │   ├── rules.py               # Правила классификации фраз
│   │   ├── curiosity.py           # Хранение непонятных вопросов
│   │   └── reinforce.py           # Подкрепление знаний (вознаграждения)
│   │
│   ├── memory/                    # Память ИИ
│   │   └── memory.py              # Работа с фактами и знаниями (JSON)
│   │
│   ├── processor/                 # Обработка текста
│   │   ├── tokenizer.py           # Токенизация и очистка текста
│   │   ├── classifier.py          # Определение типа фразы
│   │   ├── parser.py              # Объединение токенизации и классификации
│   │   └── spellchecker.py        # Орфографические исправления
│   │
│   └── inputs/                    # Ввод внешней информации
│       ├── text_input.py          # Заглушка (будущий интерфейс)
│       └── html_scraper.py        # Получение текста с веб-страниц (BeautifulSoup)
│
├── tools/                         # Утилиты и консольные инструменты
│   └── dev_console_ext.py         # Расширенная консоль с командами (/help, /mem и т.д.)
│
├── data/                          # Память и знания
│   ├── memory.json                # Факты (subject → predicate → object)
│   └── knowledge_base.json        # Знания (title, content, tags, type)
│
├── tests/                         # Pytest-тесты для всех модулей
│   ├── test_tokenizer.py
│   ├── test_classifier.py
│   ├── test_memory.py
│   ├── test_rules.py
│   ├── test_spellchecker.py
│   ├── test_utils.py
│   ├── test_curiosity.py
│   ├── test_parser.py
│   ├── test_reinforce.py
│   ├── test_text_input.py
│   └── test_html_scraper.py
│
├── requirements.txt              # Зависимости проекта (pyspellchecker, pytest и др.)
└── README.md                     # Описание проекта, запуск, структура

---
## 📊 Прогресс
Фундамент обучаемости реализован частично.  
Прогресс: **0.00% – 8.50%**

## 🧪 Тестирование
- Используется `pytest`
- Покрытие: 93%+
- Тестируются все основные модули

## ⚙️ Ограничения
- Все модули разбиты по файлам до 300 строк
- Код должен быть легко расширяем и модульно изолирован
- Переходы между сессиями/чатами не должны терять состояние проекта

## 🔄 Предполагаемые следующие этапы
- Автосбор знаний из интернета
- Механизмы генерации вопросов и выводов
- Самоинициируемое поведение
- Генерация ответов
- Telegram-интерфейс
- Механизм самообъяснения
- Эмоциональные и логические связки

### 🧠 **Этапы Развития Самообучающегося ИИ** (до полной автономии)

| Этап | Название                                                | Строк кода | Прогресс %      | Размер (ГБ) | Описание                                                         |
| ---- | ------------------------------------------------------- | ---------- | --------------- | ----------- | ---------------------------------------------------------------- |
| 1️⃣  | **Ядро ИИ** (архитектура, память, анализ)               | 10,000     | **0.00–10.0%**  | \~0.05–0.1  | Хранение и структурное осознание текста. Базовая память.         |
| 2️⃣  | **Анализ текста** (лексика, синтаксис, логика)          | 15,000     | **10.0–22.0%**  | \~0.2       | Модуль разбора текста, токенизация, классификация намерений.     |
| 3️⃣  | **Обучаемость** (память, обобщение, сохранение знаний)  | 25,000     | **22.0–40.0%**  | \~0.3–0.5   | Умение делать выводы, связывать данные, обучаться на опыте.      |
| 4️⃣  | **Любознательность** (поиск, исследование, вопросы)     | 20,000     | **40.0–56.0%**  | \~0.4       | Поиск информации на сайтах, генерация уточняющих вопросов.       |
| 5️⃣  | **Автономия мышления** (планирование, саморефлексия)    | 30,000     | **56.0–76.0%**  | \~0.6       | Целеполагание, приоритизация, корректировка стратегии.           |
| 6️⃣  | **Рефакторинг себя** (перепись модулей, структуризация) | 35,000     | **76.0–90.0%**  | \~0.7–1.0   | Самоанализ, переписывание и улучшение собственных компонентов.   |
| 7️⃣  | **Минимум внешней зависимости**                         | 10,000     | **90.0–96.0%**  | \~0.2       | Удаление сторонних библиотек, переход на собственные реализации. |
| 8️⃣  | **Полная автономия** (без внешних ИИ, без библиотек)    | 10,000     | **96.0–100.0%** | \~0.2       | ИИ полагается только на свои алгоритмы, сам обучается и растёт.  |
| Этап | Название           | Строк кода      | Длительность (при активной работе) | Размер в ГБ (код + знания) |
| ---- | ------------------ | --------------- | ---------------------------------- | -------------------------- |
| 1    | **Bootstrap**      | 5 000 – 10 000  | 1–2 недели                         | \~0.05 – 0.2 ГБ            |
| 2    | **Ядро понимания** | 10 000 – 20 000 | 2–4 недели                         | \~0.2 – 0.5 ГБ             |
| 3    | **Самообучение**   | 20 000 – 40 000 | 1–2 месяца                         | \~0.5 – 1.5 ГБ             |
| 4    | **Автономность**   | 30 000 – 80 000 | 2–3 месяца                         | \~1.5 – 3.5 ГБ             |


| Компонент                                    | Описание                                           | Примерный объём       |
| -------------------------------------------- | -------------------------------------------------- | --------------------- |
| 🔍 Анализ и интерпретация кода               | Чтение AST, поиск функций, сопоставление изменений | 15 000 – 30 000 строк |
| 🧠 Логика обучения и адаптации               | Чтение, семантический разбор, формализация         | 20 000 – 50 000 строк |
| 🛠 Автогенерация и интеграция кода           | Создание новых модулей, тестирование               | 20 000 – 60 000 строк |
| 🗂 Хранилище знаний                          | База данных знаний, кэш, версия                    | 10 000 – 25 000 строк |
| 🤖 Механизм "любопытства" и самопланирования | Что хочет изучить, почему, как                     | 10 000 – 30 000 строк |
| 🧪 Самотестирование                          | Автотесты, тестовые среды, отмена изменений        | 10 000 – 30 000 строк |
| 🧾 Документация, отчёты, мета-журнал         | Самоописание, история изменений                    | 5 000 – 10 000 строк  |

===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\.pytest_cache\README.md =====

# pytest cache directory #

This directory contains data from the pytest's cache plugin,
which provides the `--lf` and `--ff` options, as well as the `cache` fixture.

**Do not** commit this to version control.

See [the docs](https://docs.pytest.org/en/stable/how-to/cache.html) for more information.


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\common\log.py =====

import logging
import os

LOG_DIR = "data/logs"
os.makedirs(LOG_DIR, exist_ok=True)

logging.basicConfig(
    filename=os.path.join(LOG_DIR, "ai.log"),
    filemode='a',
    format='%(asctime)s | %(levelname)s | %(message)s',
    level=logging.DEBUG
)

def log_info(message: str):
    logging.info(message)

def log_error(message: str):
    logging.error(message)

def log_debug(message: str):
    logging.debug(message)


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\common\types.py =====

from dataclasses import dataclass
from enum import Enum
from typing import List, Dict, Any, Optional


class PhraseType(Enum):
    """Типы фраз, которые может обрабатывать ИИ."""
    STATEMENT = "statement"    # Утверждение
    QUESTION = "question"      # Вопрос
    COMMAND = "command"        # Команда
    UNKNOWN = "unknown"        # Неопределено


class KnowledgeType(Enum):
    """Типы знаний, сохраняемых в память."""
    FACT = "fact"
    CONCEPT = "concept"
    DEFINITION = "definition"
    RULE = "rule"
    EVENT = "event"


@dataclass
class Phrase:
    """Структура входной фразы."""
    text: str
    tokens: List[str]
    phrase_type: PhraseType
    intent: Optional[str] = None
    metadata: Optional[Dict[str, Any]] = None


@dataclass
class Fact:
    """Факт, который можно сохранить в память."""
    id: str
    subject: str
    predicate: str
    obj: str
    source: Optional[str]
    timestamp: str


@dataclass
class KnowledgeEntry:
    """Хранилище произвольной информации."""
    id: str
    title: str
    content: str
    type: KnowledgeType
    tags: List[str]
    created: str


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\common\utils.py =====

# core/common/utils.py
import json
import os
import re
import uuid
from typing import List
from datetime import datetime, timezone

def resource_path(relative_path: str) -> str:
    """
    Абсолютный путь относительно project_root (корня проекта).
    Работает, даже если скрипт запущен из поддиректории (например, tools/).
    """
    base_path = os.path.abspath(os.path.join(os.path.dirname(__file__), "../../"))  # из core/common → project_root
    return os.path.join(base_path, relative_path)

def clean_text(text: str) -> str:
    """
    Очистка текста:
    - нормализует все виды тире, дефисов и слэшей в один символ '-'
    - убирает повторяющиеся тире
    - удаляет лишние символы, кроме дефисов и пробелов
    - приводит всё к нижнему регистру
    """
    # Замена разных тире/черточек/слешей на дефис
    text = text.replace('—', '-').replace('–', '-').replace('―', '-').replace('/', '-')
    
    # Удаление лишних тире подряд: "--" → "-"
    text = re.sub(r'-{2,}', '-', text)
    
    # Удаление символов кроме букв, цифр, дефиса и пробела
    text = re.sub(r'[^\w\s-]', '', text)

    # Сжатие пробелов
    return re.sub(r'\s+', ' ', text).strip().lower()

def normalize_whitespace(text: str) -> str:
    """Приводит все пробелы к одному."""
    return re.sub(r'\s+', ' ', text).strip()

def is_ignorable(text: str) -> bool:
    """
    Определяет, является ли текст командой (/help), пустым или техническим вводом (> prompt).
    Такие строки игнорируются при обучении.
    """
    text = text.strip()
    return not text or text.startswith("/") or text.startswith(">")

def generate_id() -> str:
    """Создаёт уникальный идентификатор."""
    return str(uuid.uuid4())

def timestamp() -> str:
    """Возвращает текущую дату и время в ISO формате."""
    return datetime.now(timezone.utc).isoformat()

def load_json(filepath: str) -> dict:
    """Загружает JSON-файл в словарь."""
    if not os.path.exists(filepath):
        return {}
    with open(filepath, 'r', encoding='utf-8') as f:
        return json.load(f)

def save_json(filepath: str, data: dict) -> None:
    """Сохраняет словарь в JSON-файл."""
    with open(filepath, 'w', encoding='utf-8') as f:
        json.dump(data, f, indent=4, ensure_ascii=False)

def pretty_print(obj):
    """Удобный вывод словаря."""
    print(json.dumps(obj, indent=4, ensure_ascii=False))

def is_valid_statement(tokens: list[str]) -> bool:
    """
    Проверка: можно ли считать утверждение осмысленным.
    Требуется хотя бы 2 слова, желательно — наличие связки.
    """
    if len(tokens) < 2:
        return False

    linking_words = {"это", "есть", "-", "—", "является", "было", "будет", "стала", "была"}
    return any(t in linking_words for t in tokens)


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\inputs\html_scraper.py =====

import requests
from bs4 import BeautifulSoup

class HtmlScraper:
    """
    Модуль для сбора текстов с HTML-страниц.
    Минимальная реализация для извлечения текста из тега <body>.
    """
    def fetch_text(self, url: str) -> str:
        try:
            response = requests.get(url)
            if response.status_code == 200:
                soup = BeautifulSoup(response.text, "html.parser")
                body = soup.body
                return body.get_text(separator=' ', strip=True) if body else ""
            else:
                print(f"[HtmlScraper] Ошибка загрузки страницы: {response.status_code}")
                return ""
        except Exception as e:
            print(f"[HtmlScraper] Исключение: {e}")
            return ""


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\inputs\text_input.py =====

class TextInput:
    """
    Модуль для получения текстового ввода от пользователя.
    Пока просто заглушка для имитации ввода.
    """
    def get_input(self) -> str:
        return input("Введите текст для ИИ: ")


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\learner\curiosity.py =====

import json
import os

class Curiosity:
    def __init__(self):
        self.unknown_phrases = []

    def add_unknown(self, phrase: str):
        if phrase not in self.unknown_phrases:
            self.unknown_phrases.append(phrase)

    def get_questions(self):
        return [f"Что значит: '{p}'" for p in self.unknown_phrases]

    def get_all(self) -> list[str]:
        return self.unknown_phrases

    def clear(self):
        self.unknown_phrases.clear()

    def save(self, path: str):
        with open(path, "w", encoding="utf-8") as f:
            json.dump(self.unknown_phrases, f, ensure_ascii=False, indent=2)

    def load(self, path: str):
        if os.path.exists(path):
            try:
                with open(path, "r", encoding="utf-8") as f:
                    content = f.read().strip()
                    if content:
                        self.unknown_phrases = json.loads(content)
                    else:
                        self.unknown_phrases = []
            except (json.JSONDecodeError, IOError) as e:
                print(f"[Curiosity] Ошибка загрузки: {e}")
                self.unknown_phrases = []


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\learner\generator.py =====

#core/learner/generator.py
from core.processor.classifier import PhraseType
from core.memory.memory import Memory
from core.common.utils import timestamp


class Generator:
    """
    Отвечает за формирование базовых реакций ИИ на входящие фразы.
    Учитывает тип фразы и доступный контекст памяти.
    """

    def __init__(self, memory: Memory = None):
        self.memory = memory

    def generate_response(self, phrase_type: PhraseType, tokens: list[str], context: dict = None) -> str:
        """
        Генерация текстового ответа на основе типа фразы и токенов
        :param phrase_type: тип фразы (вопрос, утверждение, команда и т.д.)
        :param tokens: список токенов фразы
        :param context: дополнительный контекст, если есть
        :return: строка с ответом ИИ
        """
        if phrase_type.name == "QUESTION":
            return self._respond_to_question(tokens)

        if phrase_type.name == "STATEMENT":
            return self._respond_to_statement(tokens)

        if phrase_type.name == "COMMAND":
            return "Понял команду, но пока не умею её выполнять."

        return "Я пока не знаю, как на это ответить."

    def _respond_to_question(self, tokens: list[str]) -> str:
        if self.memory and tokens:
            keyword = tokens[-1]  # например, "энергия" или "кот"
            found = self.memory.search_fact_by_token(keyword)
            if found:
                return f"Вот что я нашёл в своей памяти: {found[0]}"
        return "Пока не знаю ответа, но запомню этот вопрос."

    def _respond_to_statement(self, tokens: list[str]) -> str:
        if self.memory and tokens:
            self.memory.add_fact(tokens[0], "is", " ".join(tokens[1:]))
        return "Хорошо, я запомнил это как утверждение."

    def ask_clarification(self, phrase: str) -> str:
        """Генерация уточняющего вопроса по непонятной фразе"""
        return f"Ты можешь объяснить, что значит: '{phrase}'?"

    def generate_acknowledgement(self) -> str:
        """Базовая генерация подтверждения получения информации."""
        return "Принято. Записал."

    def generate_error(self, info: str = "") -> str:
        return f"Возникла ошибка. {info}"

    def describe(self) -> str:
        return "Я — генератор ответов. Я формирую базовые реакции на вопросы, утверждения и команды."

    def debug_info(self) -> str:
        return f"[Generator@{timestamp()}] memory: {'on' if self.memory else 'off'}"


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\learner\reinforce.py =====

class Reinforce:
    """
    Модуль для подкрепления обучения ИИ.
    Пока заглушка — сюда можно добавить логику усиления изученных знаний,
    например, усиление весов правил или фактов по частоте использования.
    """
    def __init__(self):
        self.rewards = {}

    def reinforce_fact(self, fact_id: str):
        self.rewards[fact_id] = self.rewards.get(fact_id, 0) + 1
        print(f"[Reinforce] Усиление факта {fact_id}: {self.rewards[fact_id]}")

    def get_reward(self, fact_id: str) -> int:
        return self.rewards.get(fact_id, 0)


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\learner\rules.py =====

from typing import List, Optional, Dict, Any
from core.common.types import PhraseType

class Rule:
    """
    Правило для интерпретации фразы и преобразования в знание или действие.
    """
    def __init__(self, name: str, condition: callable, action: callable):
        self.name = name
        self.condition = condition  # Функция: принимает (phrase_type, tokens, raw_text), возвращает bool
        self.action = action        # Функция: принимает (phrase_type, tokens, raw_text), возвращает результат


class RulesEngine:
    def __init__(self):
        self.rules: List[Rule] = []

    def add_rule(self, rule: Rule):
        self.rules.append(rule)

    def apply_rules(self, phrase_type: PhraseType, tokens: List[str], raw_text: str) -> Optional[Dict[str, Any]]:
        """
        Применяет правила к фразе. Возвращает результат первого применимого правила.
        """
        for rule in self.rules:
            if rule.condition(phrase_type, tokens, raw_text):
                return rule.action(phrase_type, tokens, raw_text)
        return None


# Условие и действие для факта с 3 и более токенами
def condition_fact_long(phrase_type: PhraseType, tokens: List[str], raw_text: str) -> bool:
    return phrase_type == PhraseType.STATEMENT and len(tokens) >= 3

def action_fact_long(phrase_type: PhraseType, tokens: List[str], raw_text: str) -> Dict[str, Any]:
    subject = tokens[0]
    predicate = tokens[1]
    obj = " ".join(tokens[2:])
    return {"type": "fact", "subject": subject, "predicate": predicate, "object": obj, "source": raw_text}

# Условие и действие для факта с ровно 2 токенами
def condition_fact_short(phrase_type: PhraseType, tokens: List[str], raw_text: str) -> bool:
    return phrase_type == PhraseType.STATEMENT and len(tokens) == 2

def action_fact_short(phrase_type: PhraseType, tokens: List[str], raw_text: str) -> Dict[str, Any]:
    subject = tokens[0]
    predicate = tokens[1]
    obj = ""  # Объекта нет
    return {"type": "fact", "subject": subject, "predicate": predicate, "object": obj, "source": raw_text}


# Инициализация движка и добавление правил в порядке приоритета
rules_engine = RulesEngine()
rules_engine.add_rule(Rule("fact_rule_long", condition_fact_long, action_fact_long))
rules_engine.add_rule(Rule("fact_rule_short", condition_fact_short, action_fact_short))
# ...внизу rules.py

def condition_question(phrase_type, tokens, raw_text):
    return phrase_type == PhraseType.QUESTION

def action_question(phrase_type, tokens, raw_text):
    return {
        "type": "question",
        "text": raw_text,
        "tokens": tokens
    }

# Добавляем правило (после фактов)
rules_engine.add_rule(Rule("question_rule", condition_question, action_question))


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\learner\trainer.py =====

#core\learner\trainer.py
from core.processor.tokenizer import Tokenizer
from core.processor.classifier import Classifier
from core.memory.memory import Memory
from core.common.types import PhraseType
from core.learner.rules import rules_engine
from core.learner.curiosity import Curiosity
from core.learner.generator import Generator
from core.thinker.planner import Planner
from core.thinker.self_report import SelfReporter
from core.processor.spellchecker import SpellCorrector
from core.common.log import log_info, log_error
from core.common.utils import resource_path
from core.thinker.meta_journal import MetaJournal
from core.common.utils import clean_text, is_ignorable 
from core.common.utils import is_valid_statement 
from core.common.utils import resource_path

class Trainer:
    def __init__(self):
        self.tokenizer = Tokenizer()
        self.classifier = Classifier()
        self.memory = Memory(
            memory_path=resource_path("data/memory.json"),
            knowledge_path=resource_path("data/knowledge_base.json")
        )
        self.curiosity = Curiosity()
        self.curiosity_path = resource_path("data/curiosity.json")
        self.curiosity.load(self.curiosity_path)
        self.curiosity.save(self.curiosity_path)
        self.generator = Generator(memory=self.memory)
        self.planner = Planner(curiosity=self.curiosity)
        self.spellchecker = SpellCorrector()
        self.reporter = SelfReporter()
        self.journal = MetaJournal()

    def process_text(self, text: str):
        """Главная точка обучения: обработка входного текста"""

        if is_ignorable(text):
            print("[Trainer] Игнор: системная команда или пустая строка.")
            return

        text = clean_text(text)  # очистка и нормализация

        phrase_type = self.classifier.classify(text)
        tokens = self.tokenizer.tokenize(text)

        if phrase_type.name == "STATEMENT" and not is_valid_statement(tokens):
            print("[Trainer] Утверждение отвергнуто как бессмысленное.")
            self.reporter.log_action("Фраза отвергнута: недостаточно структуры.")
            return

        print(f"[Trainer] Тип фразы: {phrase_type.name}")
        print(f"[Trainer] Токены: {tokens}")

        corrected = self.spellchecker.correct_tokens(tokens)
        if corrected != tokens:
            print(f"[Spell] Исправлено: {tokens} → {corrected}")
            tokens = corrected

        result = rules_engine.apply_rules(phrase_type, tokens, text)
        if result:
            if result.get("type") == "fact":
                added = self.memory.add_fact(
                    result["subject"],
                    result["predicate"],
                    result["object"],
                    source=result.get("source")
                )
                if added:
                    msg = f"Факт: {result['subject']} — {result['predicate']} — {result['object']}"
                    print(f"[Trainer] Запомнено: {msg}")
                    log_info(f"Факт добавлен: {msg}")
                    self.reporter.log_action(f"Запомнил факт: {msg}")
                    self.journal.record_fact_added(result['subject'], result['predicate'], result['object'])

            elif result.get("type") == "question":
                self.reporter.log_action(f"Обнаружен вопрос: {result['text']}")
                self.journal.record_entry(f"Обнаружен вопрос: {result['text']}", tag="question")
                self.curiosity.add_unknown(result["text"])
                self.journal.record_goal(f"Что значит: '{result['text']}'")  # ⬅ это и будет целью
                print(f"[Trainer] Вопрос: {result['text']}")
                log_info(f"Вопрос: {result['text']}")

            elif result.get("type") == "concept":
                self.memory.add_knowledge(
                    title=result["title"],
                    content=result["content"],
                    k_type=result["k_type"],
                    tags=result.get("tags", [])
                )
                self.reporter.log_action(f"Добавлено знание: {result['title']}")
                self.journal.record_entry(f"Новое знание: {result['title']}", tag="knowledge")
                print(f"[Trainer] Сохранено как знание: {result['title']}")
    
        else:
            self.curiosity.add_unknown(text)
            print("[Trainer] Непонятная фраза — добавлена в список вопросов.")
            log_error(f"Не удалось обработать: {text}")
            self.reporter.log_action("Неопознанная фраза добавлена в любознательность")
            self.journal.record_unknown_phrase(text)

            # Уточняющий вопрос
            question = self.generator.ask_clarification(text)
            print(f"[Generator] Уточняющий вопрос: {question}")
            self.journal.record_entry(question, tag="clarification")
            self.journal.record_goal(question)  # ⬅ добавь это

        response = self.generator.generate_response(phrase_type, tokens)
        print(f"[Generator] Ответ: {response}")
        self.reporter.log_action(f"Ответ: {response}", source="Generator")
        self.journal.record_response(response)
        self.curiosity.save(self.curiosity_path)

    def plan_learning(self):
        """Получить список ближайших задач для изучения"""
        plans = self.planner.plan()
        for task in plans:
            self.journal.record_goal(task)
        return plans


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\memory\memory.py =====

import os
from typing import List, Optional
from core.common.utils import load_json, save_json, generate_id, timestamp
from core.common.types import Fact, KnowledgeEntry, KnowledgeType
from core.common.utils import resource_path

MEMORY_FILE = resource_path("data/memory.json")
KNOWLEDGE_FILE = resource_path("data/knowledge_base.json")


class Memory:
    def __init__(self, memory_path: Optional[str] = None, knowledge_path: Optional[str] = None):
        self.memory_path = memory_path or MEMORY_FILE
        self.knowledge_path = knowledge_path or KNOWLEDGE_FILE

        self.facts = self._load_facts()
        self.knowledge = self._load_knowledge()

    def _load_facts(self) -> List[Fact]:
        if not os.path.exists(self.memory_path):
            # Если файл не существует, возвращаем пустой список
            return []
        
        data = load_json(self.memory_path)
        return [Fact(**f) for f in data.get("facts", [])]

    def _load_knowledge(self) -> List[KnowledgeEntry]:
        if not os.path.exists(self.knowledge_path):
            # Если файл не существует, возвращаем пустой список
            return []
        
        data = load_json(self.knowledge_path)
        return [KnowledgeEntry(**k) for k in data.get("entries", [])]

    def _save_facts(self):
        data = {"facts": [f.__dict__ for f in self.facts]}
        save_json(self.memory_path, data)

    def _save_knowledge(self):
        data = {
            "entries": [
                {
                    **k.__dict__,
                    "type": k.type.value if hasattr(k.type, "value") else k.type
                }
                for k in self.knowledge
            ]
        }
        save_json(self.knowledge_path, data)

    def add_fact(self, subject: str, predicate: str, obj: str, source: Optional[str] = None) -> bool:
        for f in self.facts:
            if f.subject == subject and f.predicate == predicate and f.obj == obj:
                print(f"[Memory] Факт уже существует: {subject} — {predicate} — {obj}")
                return False

        fact = Fact(
            id=generate_id(),
            subject=subject,
            predicate=predicate,
            obj=obj,
            source=source,
            timestamp=timestamp()
        )
        self.facts.append(fact)
        self._save_facts()
        print(f"[Memory] Новый факт добавлен: {subject} — {predicate} — {obj}")
        return True

    def add_knowledge(self, title: str, content: str, k_type: KnowledgeType, tags: List[str]):
        entry = KnowledgeEntry(
            id=generate_id(),
            title=title,
            content=content,
            type=k_type,
            tags=tags,
            created=timestamp()
        )
        self.knowledge.append(entry)
        self._save_knowledge()

    def search_fact_by_token(self, token: str) -> List[str]:
        token = token.lower().strip()
        return [
            f"{f.subject} — {f.predicate} — {f.obj}"
            for f in self.facts
            if token == f.subject.lower() or token == f.obj.lower()
        ]



    def find_facts_by_subject(self, subject: str) -> List[Fact]:
        return [f for f in self.facts if f.subject == subject]

    def find_knowledge_by_tag(self, tag: str) -> List[KnowledgeEntry]:
        return [k for k in self.knowledge if tag in k.tags]


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\memory\recall.py =====

from core.memory.memory import Memory

class Recall:
    """
    Модуль для извлечения и поиска информации из памяти.
    Делегирует функции объекту Memory.
    """
    def __init__(self):
        self.memory = Memory()

    def recall_facts(self, subject: str):
        return self.memory.find_facts_by_subject(subject)

    def recall_knowledge(self, tag: str):
        return self.memory.find_knowledge_by_tag(tag)


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\memory\update.py =====

from core.memory.memory import Memory
from core.common.types import Fact

class UpdateMemory:
    """
    Модуль обновления существующих фактов в памяти.
    Пока реализована простая замена факта по ID.
    """
    def __init__(self):
        self.memory = Memory()

    def update_fact(self, fact_id: str, new_subject: str, new_predicate: str, new_obj: str):
        updated = False
        for i, fact in enumerate(self.memory.facts):
            if fact.id == fact_id:
                self.memory.facts[i] = Fact(fact_id, new_subject, new_predicate, new_obj, fact.source, fact.timestamp)
                updated = True
                break
        if updated:
            self.memory._save_facts()
            print(f"[UpdateMemory] Факт {fact_id} обновлен.")
        else:
            print(f"[UpdateMemory] Факт {fact_id} не найден.")


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\processor\classifier.py =====

# core/processor/classifier.py

from core.common.types import PhraseType


class Classifier:
    def __init__(self):
        self.question_starters = {"что", "кто", "где", "почему", "зачем", "как", "когда", "сколько", "можно", "ли"}
        self.command_verbs = {"расскажи", "покажи", "скажи", "объясни", "запомни", "ответь", "найди"}

    def classify(self, text: str) -> PhraseType:
        lowered = text.strip().lower()
        if not text:
            return PhraseType.UNKNOWN
        if text.startswith("/"):
            return PhraseType.COMMAND
        if all(c == '?' for c in lowered):
            return PhraseType.UNKNOWN

        if lowered.endswith("?"):
            return PhraseType.QUESTION

        if any(lowered.startswith(q) for q in self.question_starters):
            return PhraseType.QUESTION

        if any(lowered.startswith(cmd) for cmd in self.command_verbs):
            return PhraseType.COMMAND

        if lowered.endswith(".") or " " in lowered:
            return PhraseType.STATEMENT

        return PhraseType.UNKNOWN


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\processor\extractor.py =====

from typing import Dict, Any, Optional

class Extractor:
    """
    Модуль для извлечения информации из токенов и текста.
    На этом этапе — простой интерфейс.
    """
    def extract_fact(self, tokens: list, raw_text: str) -> Optional[Dict[str, Any]]:
        if len(tokens) < 3:
            return None
        return {
            "subject": tokens[0],
            "predicate": tokens[1],
            "object": " ".join(tokens[2:]),
            "source": raw_text
        }


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\processor\parser.py =====

# core/processor/parser.py

from core.common.types import Phrase, PhraseType
from core.processor.tokenizer import Tokenizer

class Parser:
    def __init__(self):
        self.tokenizer = Tokenizer()

    def parse(self, text: str) -> Phrase:
        tokens = self.tokenizer.tokenize(text)
        phrase_type = PhraseType.STATEMENT  # временно по умолчанию
        return Phrase(text=text, tokens=tokens, phrase_type=phrase_type)


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\processor\spellchecker.py =====

from spellchecker import SpellChecker

class SpellCorrector:
    def __init__(self, language="ru"):
        self.spell = SpellChecker(language=language)

    def correct_tokens(self, tokens: list) -> list:
        corrected = []
        for word in tokens:
            correction = self.spell.correction(word)
            corrected.append(correction if correction else word)
        return corrected


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\processor\tokenizer.py =====

import re
from typing import List
from core.common.utils import clean_text


class Tokenizer:
    def __init__(self):
        # Можно позже добавить список стоп-слов
        self.delimiters = r"[ \t\n\r\f\v.,!?;:\"()\-—]+"  # Разделители слов

    def tokenize(self, text: str) -> List[str]:
        """Разбивает строку на токены."""
        cleaned = clean_text(text)
        tokens = re.split(self.delimiters, cleaned)
        return [t for t in tokens if t]  # Убираем пустые строки

    def count_tokens(self, text: str) -> int:
        """Подсчитывает количество токенов."""
        return len(self.tokenize(text))

    def preview_tokens(self, text: str) -> None:
        """Выводит токены в консоль для отладки."""
        tokens = self.tokenize(text)
        print(f"[Tokenizer] → {tokens}")


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\thinker\meta_journal.py =====

import os
import ast
from core.common.utils import timestamp, resource_path

class MetaJournal:
    """
    Хранит размышления ИИ о себе, целях, ощущениях прогресса и самоанализе.
    Используется для развития внутренней осознанности.
    """

    def __init__(self, path: str = None):
        self.path = path or resource_path("data/meta_journal.json")
        os.makedirs(os.path.dirname(self.path), exist_ok=True)

    def record_entry(self, thought: str, tag: str = "general"):
        """
        Добавляет новую запись в журнал
        :param thought: размышление, наблюдение, внутреннее ощущение
        :param tag: категория (например: goal, error, reflection)
        """
        entry = {
            "time": timestamp(),
            "tag": tag,
            "thought": thought
        }
        with open(self.path, "a", encoding="utf-8") as f:
            f.write(str(entry) + "\n")

    def record_fact_added(self, subject: str, predicate: str, obj: str):
        self.record_entry(
            f"Я запомнил факт: {subject} — {predicate} — {obj}.",
            tag="fact"
        )

    def record_unknown_phrase(self, phrase: str):
        self.record_entry(
            f"Я не понял фразу и сохранил её для изучения: '{phrase}'",
            tag="curiosity"
        )

    def record_response(self, response: str):
        self.record_entry(
            f"Я ответил: {response}",
            tag="response"
        )

    def record_goal(self, description: str):
        self.record_entry(
            f"Цель: {description}",
            tag="goal"
        )

    def record_reflection(self, insight: str):
        self.record_entry(
            f"Размышление: {insight}",
            tag="reflection"
        )

    def get_recent_entries(self, limit: int = 5) -> list[str]:
        if not os.path.exists(self.path):
            return []
        with open(self.path, "r", encoding="utf-8") as f:
            lines = f.readlines()
        return lines[-limit:]

    def get_logged_goals(self) -> list[str]:
        """Вернуть список мыслей из журнала с тегом 'goal'"""
        if not os.path.exists(self.path):
            return []
    
        goals = []
        with open(self.path, "r", encoding="utf-8") as f:
            for line in f:
                try:
                    entry = ast.literal_eval(line.strip())
                    if isinstance(entry, dict) and entry.get("tag") == "goal":
                        goals.append(entry["thought"])
                except Exception as e:
                    print(f"[MetaJournal] Ошибка при чтении записи: {e}")
                    continue
        return goals

    def describe(self) -> str:
        return "Я веду мета-журнал, в котором размышляю над своими действиями и развитием."

    def debug_info(self) -> str:
        return f"[MetaJournal@{timestamp()}] журнал доступен: {'Да' if os.path.exists(self.path) else 'Нет'}"


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\thinker\planner.py =====

from core.learner.curiosity import Curiosity
from core.common.utils import timestamp


class Planner:
    def __init__(self, curiosity):
        self.curiosity = curiosity
        from core.thinker.meta_journal import MetaJournal
        self.journal = MetaJournal()

    def plan(self) -> list[str]:
        questions = self.curiosity.get_questions()
        if not questions:
            return ["На данный момент у меня нет открытых вопросов."]

        existing_goals = self.journal.get_logged_goals()
        new_goals = [q for q in questions if q not in existing_goals]

        return new_goals or ["На данный момент у меня нет открытых вопросов."]

    def describe(self) -> str:
        return "Я планирую, что стоит изучить дальше, опираясь на список непонятых фраз."

    def debug_info(self) -> str:
        return f"[Planner@{timestamp()}] вопросов в очереди: {len(self.curiosity.get_all())}"


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\core\thinker\self_report.py =====

import os
from core.common.utils import timestamp, resource_path


class SelfReporter:
    """
    Логирует действия и внутренние события ИИ в текстовый лог-файл.
    Используется для отслеживания поведения и самоанализа.
    """

    def __init__(self, log_path: str = None):
        self.log_path = log_path or resource_path("data/self_log.txt")
        os.makedirs(os.path.dirname(self.log_path), exist_ok=True)

    def log_action(self, action: str, source: str = "Trainer"):
        """
        Запись события в журнал
        :param action: описание действия
        :param source: инициатор (модуль, компонент и т.п.)
        """
        entry = f"[{timestamp()}] [{source}] {action}\n"
        with open(self.log_path, "a", encoding="utf-8") as f:
            f.write(entry)

    def get_recent_logs(self, limit: int = 10) -> list[str]:
        if not os.path.exists(self.log_path):
            return []
        with open(self.log_path, "r", encoding="utf-8") as f:
            lines = f.readlines()
        return lines[-limit:]

    def describe(self) -> str:
        return "Я фиксирую действия и внутренние события ИИ для целей самоанализа."


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\data\curiosity.json =====

[
  "кто такой я"
]

===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\data\knowledge_base.json =====

{
    "entries": [
        {
            "id": "6a2b92d5-003d-4677-9ac8-6dcfa911ca99",
            "title": "Тест",
            "content": "Описание",
            "type": "fact",
            "tags": [
                "тест"
            ],
            "created": "2025-06-25T19:47:05.917364"
        },
        {
            "id": "e41b0ce8-843b-4715-a75e-1fde8280b642",
            "title": "Тест",
            "content": "Описание",
            "type": "fact",
            "tags": [
                "тест"
            ],
            "created": "2025-06-25T19:50:27.315605+00:00"
        },
        {
            "id": "7d57d090-0c66-4c30-8e6a-c9d19044255a",
            "title": "Тест",
            "content": "Описание",
            "type": "fact",
            "tags": [
                "тест"
            ],
            "created": "2025-06-25T19:53:26.690630+00:00"
        }
    ]
}

===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\data\memory.json =====

{
    "facts": [
        {
            "id": "22222222-2222-2222-2222-222222222222",
            "subject": "вода",
            "predicate": "состоит из",
            "obj": "водорода и кислорода",
            "source": "начальные знания",
            "timestamp": "2025-06-25T12:00:00"
        },
        {
            "id": "5d113417-ab1b-42d5-93a5-c1e703651742",
            "subject": "деревья",
            "predicate": "могут",
            "obj": "гореть",
            "source": "Деревья могут гореть",
            "timestamp": "2025-06-25T18:18:49.854477"
        },
        {
            "id": "4e447987-563c-4607-99b2-0db4dec3e842",
            "subject": "Тест",
            "predicate": "есть",
            "obj": "данные",
            "source": "тест",
            "timestamp": "2025-06-25T19:26:35.762239"
        },
        {
            "id": "a8c0063d-1108-4741-af2a-000df871027f",
            "subject": "солнце",
            "predicate": "это",
            "obj": "звезда",
            "source": "Солнце это звезда",
            "timestamp": "2025-06-25T19:58:12.328126+00:00"
        },
        {
            "id": "d13045a9-9903-4c03-aa28-4cbc3f78a3aa",
            "subject": "тест",
            "predicate": "есть",
            "obj": "пример",
            "source": null,
            "timestamp": "2025-06-28T14:28:37.026743+00:00"
        },
        {
            "id": "9ec2630a-eb21-45bb-8ece-afa56eef8c41",
            "subject": "солнце",
            "predicate": "is",
            "obj": "это звезда",
            "source": null,
            "timestamp": "2025-06-30T19:30:09.964193+00:00"
        },
        {
            "id": "caf385c8-ac46-4f2a-aa2b-e76b3c8dd833",
            "subject": "блимбазоид",
            "predicate": "коллапсировал",
            "obj": "метасферу",
            "source": "блимбазоид коллапсировал метасферу",
            "timestamp": "2025-06-30T20:07:54.574359+00:00"
        },
        {
            "id": "89fcf6dc-1c60-47f2-8834-d6467ea71a7c",
            "subject": "блимбазоид",
            "predicate": "is",
            "obj": "коллапсировал метасферу",
            "source": null,
            "timestamp": "2025-06-30T20:07:54.578938+00:00"
        },
        {
            "id": "c3607bd0-790a-4119-8fda-a36d7489cdbd",
            "subject": "plan",
            "predicate": "is",
            "obj": "",
            "source": null,
            "timestamp": "2025-06-30T20:08:07.085070+00:00"
        },
        {
            "id": "90030dac-10a2-4027-8da9-d0c95baead66",
            "subject": "кот",
            "predicate": "это",
            "obj": "животное",
            "source": "кот это животное",
            "timestamp": "2025-07-03T12:32:31.778092+00:00"
        },
        {
            "id": "e972dc96-8673-4cc9-a72a-6ceaa78db892",
            "subject": "кот",
            "predicate": "is",
            "obj": "это животное",
            "source": null,
            "timestamp": "2025-07-03T12:32:31.856519+00:00"
        },
        {
            "id": "2b60b4cd-0881-46e6-b511-e6fc1576ea47",
            "subject": "слон",
            "predicate": "это",
            "obj": "животное",
            "source": "слон это животное",
            "timestamp": "2025-07-03T12:35:34.397028+00:00"
        },
        {
            "id": "134e80ee-0f18-4206-bdb2-509e57388f7c",
            "subject": "слон",
            "predicate": "is",
            "obj": "это животное",
            "source": null,
            "timestamp": "2025-07-03T12:35:34.425334+00:00"
        },
        {
            "id": "e27fe439-25c7-45a4-aeb1-6d880796c70c",
            "subject": "гримпфацианский",
            "predicate": "коллапс",
            "obj": "",
            "source": "гримпфацианский коллапсон",
            "timestamp": "2025-07-03T12:36:20.909891+00:00"
        },
        {
            "id": "f0f677f2-2c41-41be-aeb3-c67bf347f20c",
            "subject": "гримпфацианский",
            "predicate": "is",
            "obj": "коллапс",
            "source": null,
            "timestamp": "2025-07-03T12:36:21.217084+00:00"
        },
        {
            "id": "78b7a285-5c26-453b-8566-24d1a411e6c5",
            "subject": "кот",
            "predicate": "животное",
            "obj": "",
            "source": "кот жв0тное",
            "timestamp": "2025-07-03T12:38:15.034313+00:00"
        },
        {
            "id": "a52c7e40-7d14-4b51-a83b-657064b764b2",
            "subject": "кот",
            "predicate": "is",
            "obj": "животное",
            "source": null,
            "timestamp": "2025-07-03T12:38:15.049804+00:00"
        },
        {
            "id": "661db9d7-dc80-4b36-a033-0f68e5c91ef9",
            "subject": "энергия",
            "predicate": "это",
            "obj": "сила",
            "source": "энергия это сила",
            "timestamp": "2025-07-03T12:56:25.363747+00:00"
        },
        {
            "id": "33d52cbc-3b97-4780-9db9-3f56445013a3",
            "subject": "энергия",
            "predicate": "is",
            "obj": "это сила",
            "source": null,
            "timestamp": "2025-07-03T12:56:25.372105+00:00"
        },
        {
            "id": "4c2f976e-cea6-4d45-83cd-e4a3b3080199",
            "subject": "я",
            "predicate": "это",
            "obj": "твой создатель",
            "source": "я - это твой создатель",
            "timestamp": "2025-07-03T15:10:47.304947+00:00"
        },
        {
            "id": "746faefd-1340-415d-80da-cef71a13b32b",
            "subject": "я",
            "predicate": "is",
            "obj": "это твой создатель",
            "source": null,
            "timestamp": "2025-07-03T15:10:47.308786+00:00"
        }
    ]
}

===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\data\meta_journal.json =====

{'time': '2025-06-30T19:30:09.967359+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-06-30T19:36:55.871550+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-06-30T19:39:11.424483+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-06-30T19:52:11.934933+00:00', 'tag': 'goal', 'thought': 'Цель: На данный момент у меня нет открытых вопросов.'}
{'time': '2025-06-30T20:07:17.154763+00:00', 'tag': 'goal', 'thought': 'Цель: На данный момент у меня нет открытых вопросов.'}
{'time': '2025-06-30T20:07:54.578044+00:00', 'tag': 'fact', 'thought': 'Я запомнил факт: блимбазоид — коллапсировал — метасферу.'}
{'time': '2025-06-30T20:07:54.581740+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-06-30T20:08:07.084553+00:00', 'tag': 'curiosity', 'thought': "Я не понял фразу и сохранил её для изучения: '> /plan'"}
{'time': '2025-06-30T20:08:07.087261+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-06-30T20:08:18.333503+00:00', 'tag': 'goal', 'thought': "Цель: Что значит: '> /plan'"}
{'time': '2025-06-30T20:10:58.225865+00:00', 'tag': 'goal', 'thought': 'Цель: На данный момент у меня нет открытых вопросов.'}
{'time': '2025-06-30T20:18:38.262374+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-07-03T12:32:31.823145+00:00', 'tag': 'fact', 'thought': 'Я запомнил факт: кот — это — животное.'}
{'time': '2025-07-03T12:32:31.864732+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-07-03T12:33:03.987894+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: что такое энергия'}
{'time': '2025-07-03T12:33:36.718609+00:00', 'tag': 'goal', 'thought': 'Цель: На данный момент у меня нет открытых вопросов.'}
{'time': '2025-07-03T12:34:12.386098+00:00', 'tag': 'curiosity', 'thought': "Я не понял фразу и сохранил её для изучения: 'debug'"}
{'time': '2025-07-03T12:34:12.387444+00:00', 'tag': 'clarification', 'thought': "Ты можешь объяснить, что значит: 'debug'?"}
{'time': '2025-07-03T12:34:12.397430+00:00', 'tag': 'response', 'thought': 'Я ответил: Я пока не знаю, как на это ответить.'}
{'time': '2025-07-03T12:35:34.405296+00:00', 'tag': 'fact', 'thought': 'Я запомнил факт: слон — это — животное.'}
{'time': '2025-07-03T12:35:34.440985+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-07-03T12:36:21.176671+00:00', 'tag': 'fact', 'thought': 'Я запомнил факт: гримпфацианский — коллапс — .'}
{'time': '2025-07-03T12:36:21.327902+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-07-03T12:38:15.048976+00:00', 'tag': 'fact', 'thought': 'Я запомнил факт: кот — животное — .'}
{'time': '2025-07-03T12:38:15.056722+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-07-03T12:48:11.095684+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: что такое энергия'}
{'time': '2025-07-03T12:48:11.128680+00:00', 'tag': 'response', 'thought': 'Я ответил: Пока не знаю ответа, но запомню этот вопрос.'}
{'time': '2025-07-03T12:48:21.076709+00:00', 'tag': 'goal', 'thought': 'Цель: На данный момент у меня нет открытых вопросов.'}
{'time': '2025-07-03T12:49:55.933431+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: кто такой кот'}
{'time': '2025-07-03T12:49:55.949836+00:00', 'tag': 'response', 'thought': 'Я ответил: Пока не знаю ответа, но запомню этот вопрос.'}
{'time': '2025-07-03T12:50:07.889256+00:00', 'tag': 'curiosity', 'thought': "Я не понял фразу и сохранил её для изучения: 'кот'"}
{'time': '2025-07-03T12:50:07.896893+00:00', 'tag': 'clarification', 'thought': "Ты можешь объяснить, что значит: 'кот'?"}
{'time': '2025-07-03T12:50:07.899743+00:00', 'tag': 'response', 'thought': 'Я ответил: Я пока не знаю, как на это ответить.'}
{'time': '2025-07-03T12:50:20.500130+00:00', 'tag': 'curiosity', 'thought': "Я не понял фразу и сохранил её для изучения: 'животное'"}
{'time': '2025-07-03T12:50:20.500978+00:00', 'tag': 'clarification', 'thought': "Ты можешь объяснить, что значит: 'животное'?"}
{'time': '2025-07-03T12:50:20.514816+00:00', 'tag': 'response', 'thought': 'Я ответил: Я пока не знаю, как на это ответить.'}
{'time': '2025-07-03T12:50:33.333717+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-07-03T12:52:20.499174+00:00', 'tag': 'goal', 'thought': 'Цель: На данный момент у меня нет открытых вопросов.'}
{'time': '2025-07-03T12:56:25.371657+00:00', 'tag': 'fact', 'thought': 'Я запомнил факт: энергия — это — сила.'}
{'time': '2025-07-03T12:56:25.385038+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-07-03T12:56:50.502603+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: что такое энергия'}
{'time': '2025-07-03T12:56:50.512660+00:00', 'tag': 'response', 'thought': 'Я ответил: Пока не знаю ответа, но запомню этот вопрос.'}
{'time': '2025-07-03T13:34:00.650146+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-07-03T13:36:47.381579+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-07-03T13:37:00.140435+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-07-03T13:39:03.486955+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-07-03T13:47:26.712540+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-07-03T13:47:54.594476+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: кто такой кот'}
{'time': '2025-07-03T13:47:54.696298+00:00', 'tag': 'response', 'thought': 'Я ответил: Пока не знаю ответа, но запомню этот вопрос.'}
{'time': '2025-07-03T13:48:03.496564+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: кто кот'}
{'time': '2025-07-03T13:48:03.533612+00:00', 'tag': 'response', 'thought': 'Я ответил: Пока не знаю ответа, но запомню этот вопрос.'}
{'time': '2025-07-03T13:48:14.677907+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: кто тест'}
{'time': '2025-07-03T13:48:14.726717+00:00', 'tag': 'response', 'thought': 'Я ответил: Пока не знаю ответа, но запомню этот вопрос.'}
{'time': '2025-07-03T13:48:50.781737+00:00', 'tag': 'curiosity', 'thought': "Я не понял фразу и сохранил её для изучения: 'кот'"}
{'time': '2025-07-03T13:48:50.805797+00:00', 'tag': 'clarification', 'thought': "Ты можешь объяснить, что значит: 'кот'?"}
{'time': '2025-07-03T13:48:50.861943+00:00', 'tag': 'response', 'thought': 'Я ответил: Я пока не знаю, как на это ответить.'}
{'time': '2025-07-03T13:48:58.351973+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-07-03T13:50:51.625745+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-07-03T13:51:01.361811+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: что такое энергия'}
{'time': '2025-07-03T13:51:01.474422+00:00', 'tag': 'response', 'thought': 'Я ответил: Пока не знаю ответа, но запомню этот вопрос.'}
{'time': '2025-07-03T13:55:22.030707+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: кто такой кот'}
{'time': '2025-07-03T13:55:22.049338+00:00', 'tag': 'response', 'thought': 'Я ответил: Вот что я нашёл в своей памяти: "Солнце" — "Солнце — - это звезда, которая является источником света и тепла для Земли." ["астрономия", "солнце",'}
{'time': '2025-07-03T13:55:44.832217+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: что такое энергия'}
{'time': '2025-07-03T13:55:44.884661+00:00', 'tag': 'response', 'thought': 'Я ответил: Вот что я нашёл в своей памяти: энергия — это — сила'}
{'time': '2025-07-03T14:00:21.188248+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: кто такой кот'}
{'time': '2025-07-03T14:00:21.304236+00:00', 'tag': 'response', 'thought': 'Я ответил: Вот что я нашёл в своей памяти: "Солнце" — "Солнце — - это звезда, которая является источником света и тепла для Земли." ["астрономия", "солнце",'}
{'time': '2025-07-03T14:01:06.823196+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: что такое солнце'}
{'time': '2025-07-03T14:01:06.893435+00:00', 'tag': 'response', 'thought': 'Я ответил: Вот что я нашёл в своей памяти: солнце — это — звезда'}
{'time': '2025-07-03T14:01:39.001456+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: что такое солнце'}
{'time': '2025-07-03T14:01:39.056685+00:00', 'tag': 'response', 'thought': 'Я ответил: Вот что я нашёл в своей памяти: солнце — это — звезда'}
{'time': '2025-07-03T14:02:31.274350+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: кто такой кот'}
{'time': '2025-07-03T14:02:31.346422+00:00', 'tag': 'response', 'thought': 'Я ответил: Вот что я нашёл в своей памяти: "Солнце" — "Солнце — - это звезда, которая является источником света и тепла для Земли." ["астрономия", "солнце",'}
{'time': '2025-07-03T14:03:11.886234+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: кто такой кот'}
{'time': '2025-07-03T14:03:11.924679+00:00', 'tag': 'response', 'thought': 'Я ответил: Вот что я нашёл в своей памяти: кот — это — животное'}
{'time': '2025-07-03T14:08:03.549742+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-07-03T14:19:50.375023+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: кто такой кот'}
{'time': '2025-07-03T14:19:50.377919+00:00', 'tag': 'response', 'thought': 'Я ответил: Вот что я нашёл в своей памяти: кот — это — животное'}
{'time': '2025-07-03T14:20:04.288182+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: что такое кот'}
{'time': '2025-07-03T14:20:04.290529+00:00', 'tag': 'response', 'thought': 'Я ответил: Вот что я нашёл в своей памяти: кот — это — животное'}
{'time': '2025-07-03T14:20:22.089620+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: кто такой солнце'}
{'time': '2025-07-03T14:20:22.092590+00:00', 'tag': 'response', 'thought': 'Я ответил: Вот что я нашёл в своей памяти: солнце — это — звезда'}
{'time': '2025-07-03T14:20:36.560559+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: что такой энергия'}
{'time': '2025-07-03T14:20:36.562452+00:00', 'tag': 'response', 'thought': 'Я ответил: Вот что я нашёл в своей памяти: энергия — это — сила'}
{'time': '2025-07-03T14:39:18.585790+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: кто такой я'}
{'time': '2025-07-03T14:39:18.588714+00:00', 'tag': 'response', 'thought': 'Я ответил: Пока не знаю ответа, но запомню этот вопрос.'}
{'time': '2025-07-03T14:39:58.841712+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: кто такой я'}
{'time': '2025-07-03T14:39:58.845454+00:00', 'tag': 'response', 'thought': 'Я ответил: Пока не знаю ответа, но запомню этот вопрос.'}
{'time': '2025-07-03T14:40:10.322132+00:00', 'tag': 'goal', 'thought': 'Цель: На данный момент у меня нет открытых вопросов.'}
{'time': '2025-07-03T14:43:38.089877+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: кто такой я'}
{'time': '2025-07-03T14:43:38.092950+00:00', 'tag': 'response', 'thought': 'Я ответил: Пока не знаю ответа, но запомню этот вопрос.'}
{'time': '2025-07-03T14:51:55.102400+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: кто такой я'}
{'time': '2025-07-03T14:51:55.105262+00:00', 'tag': 'response', 'thought': 'Я ответил: Пока не знаю ответа, но запомню этот вопрос.'}
{'time': '2025-07-03T14:52:19.670500+00:00', 'tag': 'goal', 'thought': 'Цель: На данный момент у меня нет открытых вопросов.'}
{'time': '2025-07-03T15:00:37.908111+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: кто такой я'}
{'time': '2025-07-03T15:00:37.910936+00:00', 'tag': 'response', 'thought': 'Я ответил: Пока не знаю ответа, но запомню этот вопрос.'}
{'time': '2025-07-03T15:00:42.020224+00:00', 'tag': 'goal', 'thought': 'Цель: На данный момент у меня нет открытых вопросов.'}
{'time': '2025-07-03T15:01:41.285747+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: кто такой'}
{'time': '2025-07-03T15:01:41.289259+00:00', 'tag': 'response', 'thought': 'Я ответил: Пока не знаю ответа, но запомню этот вопрос.'}
{'time': '2025-07-03T15:01:46.255801+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: кто такой я'}
{'time': '2025-07-03T15:01:46.257771+00:00', 'tag': 'response', 'thought': 'Я ответил: Пока не знаю ответа, но запомню этот вопрос.'}
{'time': '2025-07-03T15:01:50.532660+00:00', 'tag': 'goal', 'thought': 'Цель: На данный момент у меня нет открытых вопросов.'}
{'time': '2025-07-03T15:04:00.603242+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: кто такой я'}
{'time': '2025-07-03T15:04:00.603630+00:00', 'tag': 'goal', 'thought': "Цель: Что значит: 'кто такой я'"}
{'time': '2025-07-03T15:04:00.606586+00:00', 'tag': 'response', 'thought': 'Я ответил: Пока не знаю ответа, но запомню этот вопрос.'}
{'time': '2025-07-03T15:08:41.073970+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: кто такой я'}
{'time': '2025-07-03T15:08:41.074444+00:00', 'tag': 'goal', 'thought': "Цель: Что значит: 'кто такой я'"}
{'time': '2025-07-03T15:08:41.077322+00:00', 'tag': 'response', 'thought': 'Я ответил: Пока не знаю ответа, но запомню этот вопрос.'}
{'time': '2025-07-03T15:08:49.871457+00:00', 'tag': 'goal', 'thought': "Цель: Что значит: 'кто такой я'"}
{'time': '2025-07-03T15:10:47.308370+00:00', 'tag': 'fact', 'thought': 'Я запомнил факт: я — это — твой создатель.'}
{'time': '2025-07-03T15:10:47.312507+00:00', 'tag': 'response', 'thought': 'Я ответил: Хорошо, я запомнил это как утверждение.'}
{'time': '2025-07-03T15:10:56.485095+00:00', 'tag': 'question', 'thought': 'Обнаружен вопрос: кто такой я'}
{'time': '2025-07-03T15:10:56.485611+00:00', 'tag': 'goal', 'thought': "Цель: Что значит: 'кто такой я'"}
{'time': '2025-07-03T15:10:56.487961+00:00', 'tag': 'response', 'thought': 'Я ответил: Вот что я нашёл в своей памяти: я — это — твой создатель'}


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\data\self_log.txt =====

[2025-06-30T19:30:09.966860+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-06-30T19:36:55.870878+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-06-30T19:39:11.421710+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-06-30T20:07:54.577514+00:00] [Trainer] Запомнил факт: Факт: блимбазоид — коллапсировал — метасферу
[2025-06-30T20:07:54.581042+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-06-30T20:08:07.083419+00:00] [Trainer] Неопознанная фраза добавлена в любознательность
[2025-06-30T20:08:07.086738+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-06-30T20:18:38.261901+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-07-03T12:32:31.813191+00:00] [Trainer] Запомнил факт: Факт: кот — это — животное
[2025-07-03T12:32:31.864192+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-07-03T12:33:03.986050+00:00] [Trainer] Обнаружен вопрос: что такое энергия
[2025-07-03T12:34:12.385522+00:00] [Trainer] Неопознанная фраза добавлена в любознательность
[2025-07-03T12:34:12.389431+00:00] [Generator] Ответ: Я пока не знаю, как на это ответить.
[2025-07-03T12:35:34.402406+00:00] [Trainer] Запомнил факт: Факт: слон — это — животное
[2025-07-03T12:35:34.440100+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-07-03T12:36:21.078667+00:00] [Trainer] Запомнил факт: Факт: гримпфацианский — коллапс — 
[2025-07-03T12:36:21.310717+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-07-03T12:38:15.048305+00:00] [Trainer] Запомнил факт: Факт: кот — животное — 
[2025-07-03T12:38:15.056041+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-07-03T12:48:11.094888+00:00] [Trainer] Обнаружен вопрос: что такое энергия
[2025-07-03T12:48:11.127994+00:00] [Generator] Ответ: Пока не знаю ответа, но запомню этот вопрос.
[2025-07-03T12:49:55.924884+00:00] [Trainer] Обнаружен вопрос: кто такой кот
[2025-07-03T12:49:55.949206+00:00] [Generator] Ответ: Пока не знаю ответа, но запомню этот вопрос.
[2025-07-03T12:50:07.888703+00:00] [Trainer] Неопознанная фраза добавлена в любознательность
[2025-07-03T12:50:07.898802+00:00] [Generator] Ответ: Я пока не знаю, как на это ответить.
[2025-07-03T12:50:20.499460+00:00] [Trainer] Неопознанная фраза добавлена в любознательность
[2025-07-03T12:50:20.512497+00:00] [Generator] Ответ: Я пока не знаю, как на это ответить.
[2025-07-03T12:50:33.332468+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-07-03T12:56:25.371122+00:00] [Trainer] Запомнил факт: Факт: энергия — это — сила
[2025-07-03T12:56:25.381654+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-07-03T12:56:50.501891+00:00] [Trainer] Обнаружен вопрос: что такое энергия
[2025-07-03T12:56:50.511498+00:00] [Generator] Ответ: Пока не знаю ответа, но запомню этот вопрос.
[2025-07-03T13:34:00.649699+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-07-03T13:36:47.381104+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-07-03T13:37:00.138162+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-07-03T13:39:03.485399+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-07-03T13:47:26.686073+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-07-03T13:47:54.585814+00:00] [Trainer] Обнаружен вопрос: кто такой кот
[2025-07-03T13:47:54.629583+00:00] [Generator] Ответ: Пока не знаю ответа, но запомню этот вопрос.
[2025-07-03T13:48:03.482615+00:00] [Trainer] Обнаружен вопрос: кто кот
[2025-07-03T13:48:03.532767+00:00] [Generator] Ответ: Пока не знаю ответа, но запомню этот вопрос.
[2025-07-03T13:48:14.677302+00:00] [Trainer] Обнаружен вопрос: кто тест
[2025-07-03T13:48:14.716530+00:00] [Generator] Ответ: Пока не знаю ответа, но запомню этот вопрос.
[2025-07-03T13:48:50.781091+00:00] [Trainer] Неопознанная фраза добавлена в любознательность
[2025-07-03T13:48:50.836020+00:00] [Generator] Ответ: Я пока не знаю, как на это ответить.
[2025-07-03T13:48:58.350951+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-07-03T13:50:51.625251+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-07-03T13:51:01.350583+00:00] [Trainer] Обнаружен вопрос: что такое энергия
[2025-07-03T13:51:01.451355+00:00] [Generator] Ответ: Пока не знаю ответа, но запомню этот вопрос.
[2025-07-03T13:55:22.030118+00:00] [Trainer] Обнаружен вопрос: кто такой кот
[2025-07-03T13:55:22.048745+00:00] [Generator] Ответ: Вот что я нашёл в своей памяти: "Солнце" — "Солнце — - это звезда, которая является источником света и тепла для Земли." ["астрономия", "солнце",
[2025-07-03T13:55:44.804237+00:00] [Trainer] Обнаружен вопрос: что такое энергия
[2025-07-03T13:55:44.864356+00:00] [Generator] Ответ: Вот что я нашёл в своей памяти: энергия — это — сила
[2025-07-03T14:00:21.153160+00:00] [Trainer] Обнаружен вопрос: кто такой кот
[2025-07-03T14:00:21.273730+00:00] [Generator] Ответ: Вот что я нашёл в своей памяти: "Солнце" — "Солнце — - это звезда, которая является источником света и тепла для Земли." ["астрономия", "солнце",
[2025-07-03T14:01:06.822638+00:00] [Trainer] Обнаружен вопрос: что такое солнце
[2025-07-03T14:01:06.892863+00:00] [Generator] Ответ: Вот что я нашёл в своей памяти: солнце — это — звезда
[2025-07-03T14:01:38.979652+00:00] [Trainer] Обнаружен вопрос: что такое солнце
[2025-07-03T14:01:39.054020+00:00] [Generator] Ответ: Вот что я нашёл в своей памяти: солнце — это — звезда
[2025-07-03T14:02:31.271444+00:00] [Trainer] Обнаружен вопрос: кто такой кот
[2025-07-03T14:02:31.318898+00:00] [Generator] Ответ: Вот что я нашёл в своей памяти: "Солнце" — "Солнце — - это звезда, которая является источником света и тепла для Земли." ["астрономия", "солнце",
[2025-07-03T14:03:11.854979+00:00] [Trainer] Обнаружен вопрос: кто такой кот
[2025-07-03T14:03:11.924094+00:00] [Generator] Ответ: Вот что я нашёл в своей памяти: кот — это — животное
[2025-07-03T14:08:03.491744+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-07-03T14:19:50.374384+00:00] [Trainer] Обнаружен вопрос: кто такой кот
[2025-07-03T14:19:50.377266+00:00] [Generator] Ответ: Вот что я нашёл в своей памяти: кот — это — животное
[2025-07-03T14:20:04.287362+00:00] [Trainer] Обнаружен вопрос: что такое кот
[2025-07-03T14:20:04.289750+00:00] [Generator] Ответ: Вот что я нашёл в своей памяти: кот — это — животное
[2025-07-03T14:20:22.088683+00:00] [Trainer] Обнаружен вопрос: кто такой солнце
[2025-07-03T14:20:22.091874+00:00] [Generator] Ответ: Вот что я нашёл в своей памяти: солнце — это — звезда
[2025-07-03T14:20:36.559806+00:00] [Trainer] Обнаружен вопрос: что такой энергия
[2025-07-03T14:20:36.561715+00:00] [Generator] Ответ: Вот что я нашёл в своей памяти: энергия — это — сила
[2025-07-03T14:39:18.585200+00:00] [Trainer] Обнаружен вопрос: кто такой я
[2025-07-03T14:39:18.588049+00:00] [Generator] Ответ: Пока не знаю ответа, но запомню этот вопрос.
[2025-07-03T14:39:58.840923+00:00] [Trainer] Обнаружен вопрос: кто такой я
[2025-07-03T14:39:58.844649+00:00] [Generator] Ответ: Пока не знаю ответа, но запомню этот вопрос.
[2025-07-03T14:43:38.089229+00:00] [Trainer] Обнаружен вопрос: кто такой я
[2025-07-03T14:43:38.092223+00:00] [Generator] Ответ: Пока не знаю ответа, но запомню этот вопрос.
[2025-07-03T14:51:55.101777+00:00] [Trainer] Обнаружен вопрос: кто такой я
[2025-07-03T14:51:55.104700+00:00] [Generator] Ответ: Пока не знаю ответа, но запомню этот вопрос.
[2025-07-03T15:00:37.907473+00:00] [Trainer] Обнаружен вопрос: кто такой я
[2025-07-03T15:00:37.910286+00:00] [Generator] Ответ: Пока не знаю ответа, но запомню этот вопрос.
[2025-07-03T15:01:41.285028+00:00] [Trainer] Обнаружен вопрос: кто такой
[2025-07-03T15:01:41.288447+00:00] [Generator] Ответ: Пока не знаю ответа, но запомню этот вопрос.
[2025-07-03T15:01:46.252691+00:00] [Trainer] Обнаружен вопрос: кто такой я
[2025-07-03T15:01:46.257062+00:00] [Generator] Ответ: Пока не знаю ответа, но запомню этот вопрос.
[2025-07-03T15:04:00.602610+00:00] [Trainer] Обнаружен вопрос: кто такой я
[2025-07-03T15:04:00.605821+00:00] [Generator] Ответ: Пока не знаю ответа, но запомню этот вопрос.
[2025-07-03T15:08:41.073368+00:00] [Trainer] Обнаружен вопрос: кто такой я
[2025-07-03T15:08:41.076793+00:00] [Generator] Ответ: Пока не знаю ответа, но запомню этот вопрос.
[2025-07-03T15:10:47.307926+00:00] [Trainer] Запомнил факт: Факт: я — это — твой создатель
[2025-07-03T15:10:47.310952+00:00] [Generator] Ответ: Хорошо, я запомнил это как утверждение.
[2025-07-03T15:10:56.484215+00:00] [Trainer] Обнаружен вопрос: кто такой я
[2025-07-03T15:10:56.487046+00:00] [Generator] Ответ: Вот что я нашёл в своей памяти: я — это — твой создатель


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\tests\test_classifier.py =====

import pytest
from core.processor.classifier import Classifier
from core.common.types import PhraseType

classifier = Classifier()

def test_classify_command():
    c = Classifier()
    assert c.classify("/run") == PhraseType.COMMAND

def test_classify_unknown():
    c = Classifier()
    assert c.classify("😳?🐍") == PhraseType.UNKNOWN

@pytest.mark.parametrize("text,expected", [
    ("Что ты знаешь?", PhraseType.QUESTION),
    ("Сколько времени?", PhraseType.QUESTION),
    ("Расскажи про солнце", PhraseType.COMMAND),
    ("Солнце — это звезда.", PhraseType.STATEMENT),
    ("???", PhraseType.UNKNOWN),
])
def test_classify(text, expected):
    assert classifier.classify(text) == expected


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\tests\test_console.py =====

import sys
import os
import builtins
import pytest

# Добавляем путь к tools/
TOOLS_PATH = os.path.abspath(os.path.join(os.path.dirname(__file__), "..", "tools"))
sys.path.insert(0, TOOLS_PATH)

import console  # теперь импортируется корректно

def fake_input_generator(inputs):
    def fake_input(_=None):
        return inputs.pop(0) if inputs else "/exit"
    return fake_input

def test_console_commands(monkeypatch):
    inputs = [
        "/test солнце это звезда",
        "/mem facts солнце",
        "/mem knowledge тест",
        "/add fact тест есть пример",
        "/mem export md",
        "/exit"
    ]
    monkeypatch.setattr(builtins, "input", fake_input_generator(inputs))
    console.main()


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\tests\test_curiosity.py =====

from core.learner.curiosity import Curiosity

def test_add_and_get_questions():
    c = Curiosity()
    c.add_unknown("Квантовая запутанность?")
    questions = c.get_questions()
    assert questions == ["Что значит: 'Квантовая запутанность?'"]

def test_clear():
    c = Curiosity()
    c.add_unknown("abc")
    c.clear()
    assert c.get_questions() == []


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\tests\test_generator.py =====

import pytest
from core.learner.generator import Generator
from core.processor.classifier import PhraseType


class DummyMemory:
    def __init__(self):
        self.facts = ["вода — жидкость"]

    def search_fact(self, query):
        return [f for f in self.facts if query in f]

    def add_fact(self, subj, pred, obj):
        self.facts.append(f"{subj} — {pred} — {obj}")
        return True


def test_generate_question():
    g = Generator(memory=DummyMemory())
    result = g.generate_response(PhraseType.QUESTION, ["вода", "жидкость"])
    assert "в своей памяти" in result or "не знаю" in result


def test_generate_statement():
    mem = DummyMemory()
    g = Generator(memory=mem)
    result = g.generate_response(PhraseType.STATEMENT, ["вода", "мокрая"])
    assert "запомнил" in result
    assert any("вода — is — мокрая" in f for f in mem.facts)


def test_generate_unknown():
    g = Generator()
    result = g.generate_response(PhraseType.UNKNOWN, ["что-то"])
    assert "не знаю" in result


def test_acknowledgement():
    g = Generator()
    assert g.generate_acknowledgement() == "Принято. Записал."


def test_error_generation():
    g = Generator()
    assert "ошибка" in g.generate_error("тест")


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\tests\test_html_scraper.py =====

import pytest
from core.inputs.html_scraper import HtmlScraper
def test_fetch_empty_html(monkeypatch):
    class FakeResponse:
        text = ""
    def fake_get(*args, **kwargs):
        return FakeResponse()
    monkeypatch.setattr("requests.get", fake_get)
    scraper = HtmlScraper()
    result = scraper.fetch_text("http://test")
    assert result == ""
@pytest.mark.skip(reason="Временно отключено,так как дохуя жрёт")
def test_fetch_text_invalid_url():
    scraper = HtmlScraper()
    result = scraper.fetch_text("http://invalid.url")
    assert isinstance(result, str)


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\tests\test_memory.py =====

import tempfile
import shutil
import os
from core.memory.memory import Memory
from core.common.types import KnowledgeType
from core.common.utils import generate_id, timestamp

def test_find_facts_by_subject(tmp_path):
    memory_file = tmp_path / "memory.json"
    knowledge_file = tmp_path / "knowledge.json"
    memory = Memory(memory_path=str(memory_file), knowledge_path=str(knowledge_file))

    # Добавляем тестовый факт
    memory.add_fact("вода", "является", "жидкостью")
    
    results = memory.find_facts_by_subject("вода")
    assert len(results) == 1
    assert results[0].subject == "вода"


def test_find_facts_by_subject(tmp_path):
    mem = Memory(
        memory_path=str(tmp_path / "memory.json"),
        knowledge_path=str(tmp_path / "knowledge.json")
    )
    mem.add_fact("вода", "является", "жидкостью")
    results = mem.find_facts_by_subject("вода")
    assert len(results) == 1

def test_memory_fact_storage():
    tmp_dir = tempfile.mkdtemp()
    memory_file = os.path.join(tmp_dir, "memory.json")
    knowledge_file = os.path.join(tmp_dir, "knowledge_base.json")

    # Создаём пустые файлы для памяти
    with open(memory_file, "w", encoding="utf-8") as f:
        f.write('{"facts": []}')
    with open(knowledge_file, "w", encoding="utf-8") as f:
        f.write('{"entries": []}')

    # Переопределяем пути
    memory = Memory(memory_path=memory_file, knowledge_path=knowledge_file)

    # Добавляем факт
    memory.add_fact("Тест", "есть", "данные", "тест")
    
    # Проверяем, что факт был добавлен
    facts = memory.find_facts_by_subject("Тест")
    assert len(facts) == 1
    assert facts[0].predicate == "есть"

    shutil.rmtree(tmp_dir)

def test_memory_knowledge_storage():
    tmp_dir = tempfile.mkdtemp()
    memory_file = os.path.join(tmp_dir, "memory.json")
    knowledge_file = os.path.join(tmp_dir, "knowledge_base.json")

    # Очистка временных файлов
    with open(memory_file, "w", encoding="utf-8") as f:
        f.write('{"facts": []}')
    with open(knowledge_file, "w", encoding="utf-8") as f:
        f.write('{"entries": []}')

    # Переопределяем пути
    memory = Memory(memory_path=memory_file, knowledge_path=knowledge_file)

    # Добавляем знания
    memory.add_knowledge("Тест", "Описание", KnowledgeType.FACT, ["тест"])
    
    # Проверяем, что знания добавлены
    found = memory.find_knowledge_by_tag("тест")
    assert len(found) == 1

    shutil.rmtree(tmp_dir)


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\tests\test_meta_journal.py =====

import os
import tempfile
from core.thinker.meta_journal import MetaJournal


def test_record_and_read_entry():
    with tempfile.TemporaryDirectory() as tmp:
        path = os.path.join(tmp, "journal.json")
        journal = MetaJournal(path)
        journal.record_entry("Проверка мысли", tag="test")

        entries = journal.get_recent_entries()
        assert any("Проверка мысли" in e for e in entries)
        assert any("'tag': 'test'" in e or 'test' in e for e in entries)


def test_record_fact_and_response():
    with tempfile.TemporaryDirectory() as tmp:
        path = os.path.join(tmp, "journal.json")
        journal = MetaJournal(path)
        journal.record_fact_added("Солнце", "является", "звездой")
        journal.record_response("Да, Солнце — звезда.")
        entries = journal.get_recent_entries()
        assert any("Солнце" in e for e in entries)
        assert any("звезда" in e for e in entries)
        assert any("ответил" in e.lower() for e in entries)


def test_empty_journal():
    with tempfile.TemporaryDirectory() as tmp:
        path = os.path.join(tmp, "empty.json")
        journal = MetaJournal(path)
        assert journal.get_recent_entries() == []


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\tests\test_parser.py =====

from core.processor.parser import Parser
from core.common.types import Phrase

def test_parse_structure():
    parser = Parser()
    phrase = parser.parse("Солнце это звезда")
    assert isinstance(phrase, Phrase)
    assert phrase.tokens == ["солнце", "это", "звезда"]  # ⬅ заменили на нижний регистр


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\tests\test_planner.py =====

import pytest
from core.thinker.planner import Planner

class DummyCuriosity:
    def __init__(self, unknowns=None):
        self._unknowns = unknowns or []

    def get_questions(self):
        return [f"Что значит: '{x}'" for x in self._unknowns]

def test_plan_with_phrases():
    curiosity = DummyCuriosity(["Что такое энергия?", "Почему небо синее?"])
    planner = Planner(curiosity=curiosity)
    plan = planner.plan()
    assert any("что значит" in p.lower() for p in plan)
    assert len(plan) == 2


def test_plan_empty():
    curiosity = DummyCuriosity([])
    planner = Planner(curiosity=curiosity)
    plan = planner.plan()
    assert plan == ["На данный момент у меня нет открытых вопросов."]


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\tests\test_reinforce.py =====

from core.learner.reinforce import Reinforce

def test_reinforce_fact():
    r = Reinforce()
    r.reinforce_fact("f123")
    assert r.get_reward("f123") == 1
    r.reinforce_fact("f123")
    assert r.get_reward("f123") == 2

def test_no_reward():
    r = Reinforce()
    assert r.get_reward("unknown") == 0


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\tests\test_rules.py =====

from core.learner.rules import rules_engine
from core.common.types import PhraseType

def test_long_fact_rule():
    result = rules_engine.apply_rules(PhraseType.STATEMENT, ["солнце", "является", "звездой"], "Солнце является звездой")
    assert result["type"] == "fact"
    assert result["subject"] == "солнце"

def test_short_fact_rule():
    result = rules_engine.apply_rules(PhraseType.STATEMENT, ["вода", "мокрая"], "Вода мокрая")
    assert result["type"] == "fact"
    assert result["subject"] == "вода"

def test_question_rule():
    result = rules_engine.apply_rules(PhraseType.QUESTION, ["что", "такое", "вода"], "Что такое вода?")
    assert result["type"] == "question"
    assert result["text"] == "Что такое вода?"

def test_no_match_rule():
    result = rules_engine.apply_rules(PhraseType.UNKNOWN, ["..."], "???")
    assert result is None


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\tests\test_run.py =====

import builtins
import pytest
from run import main

def fake_input_generator(inputs):
    """Генератор фейкового ввода вместо input()"""
    def fake_input(_=None):
        return inputs.pop(0) if inputs else "/exit"
    return fake_input

def test_run_main(monkeypatch):
    # Подготовка данных для симуляции
    inputs = [
        "Солнце — это звезда",  # фраза для обработки
        "/exit"                # выход
    ]
    monkeypatch.setattr(builtins, "input", fake_input_generator(inputs))
    main()  # просто вызываем main(), без assert — тест на отсутствие сбоев


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\tests\test_self_reporter.py =====

import os
import tempfile
from core.thinker.self_report import SelfReporter


def test_log_action_and_retrieve():
    with tempfile.TemporaryDirectory() as tmp:
        path = os.path.join(tmp, "log.txt")
        sr = SelfReporter(log_path=path)
        sr.log_action("Проверка записи", source="Test")

        logs = sr.get_recent_logs()
        assert any("Проверка записи" in line for line in logs)
        assert any("Test" in line for line in logs)


def test_log_file_created():
    with tempfile.TemporaryDirectory() as tmp:
        path = os.path.join(tmp, "log.txt")
        sr = SelfReporter(log_path=path)
        sr.log_action("Создание файла")
        assert os.path.exists(path)


def test_empty_log():
    with tempfile.TemporaryDirectory() as tmp:
        path = os.path.join(tmp, "empty.txt")
        sr = SelfReporter(log_path=path)
        logs = sr.get_recent_logs()
        assert logs == []


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\tests\test_spellchecker.py =====

from core.processor.spellchecker import SpellCorrector

def test_correct_known_word():
    sc = SpellCorrector()
    assert sc.correct_tokens(["помощнык"]) == ["помощник"]

def test_does_not_change_correct():
    sc = SpellCorrector()
    assert sc.correct_tokens(["друзья"]) == ["друзья"]

def test_mixed_batch():
    sc = SpellCorrector()
    assert sc.correct_tokens(["лучшые", "помощнык", "мы"]) == ["лучше", "помощник", "мы"]


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\tests\test_text_input.py =====

from core.inputs.text_input import TextInput

def test_text_input_stub():
    ti = TextInput()
    assert hasattr(ti, "get_input")


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\tests\test_tokenizer.py =====

import pytest
from core.processor.tokenizer import Tokenizer

tokenizer = Tokenizer()

def test_count_tokens():
    t = Tokenizer()
    assert t.count_tokens("Это тест") == 2

def test_preview_tokens(capsys):
    t = Tokenizer()
    t.preview_tokens("Пример текста")
    captured = capsys.readouterr()
    assert "пример" in captured.out.lower()

def test_clean_text():
    from core.common.utils import clean_text
    assert clean_text("Привет, мир!!!") == "привет мир"

def test_split_symbols():
    t = Tokenizer()
    result = t.tokenize("Ты — мой друг.")
    assert "друг" in result

def test_tokenize_basic():
    text = "Привет, мир!"
    tokens = tokenizer.tokenize(text)
    assert tokens == ["привет", "мир"]

def test_tokenize_empty():
    assert tokenizer.tokenize("") == []

def test_tokenize_whitespace():
    assert tokenizer.tokenize("   \n\t") == []


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\tests\test_utils.py =====

from core.common.utils import clean_text, normalize_whitespace, generate_id, timestamp
import os
from core.common.utils import save_json, load_json

def test_save_and_load_json(tmp_path):
    filepath = tmp_path / "test.json"
    data = {"a": 1}
    save_json(str(filepath), data)
    loaded = load_json(str(filepath))
    assert loaded == data

def test_clean_text():
    text = " Привет, мир!!! "
    assert clean_text(text) == "привет мир"

def test_normalize_whitespace():
    text = "это   тест\n\nстроки"
    assert normalize_whitespace(text) == "это тест строки"

def test_generate_id_unique():
    id1 = generate_id()
    id2 = generate_id()
    assert id1 != id2 and isinstance(id1, str)

def test_timestamp_format():
    ts = timestamp()
    assert "T" in ts  # ISO формат


===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\tests\__init__.py =====



===== FILE: C:\Users\Brazers\Desktop\Endi-IA\P v0.2-r-b\tools\console.py =====

import sys, os, csv, subprocess, traceback
sys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), "..")))

from core.memory.memory import Memory
from core.processor.tokenizer import Tokenizer
from core.processor.classifier import Classifier
from core.common.types import KnowledgeType
from core.learner.trainer import Trainer
from run import main as run_main

def install_requirements():
    """Устанавливает все необходимые библиотеки из requirements.txt."""
    if not os.path.exists('requirements.txt'):
        print("❌ Не найден файл requirements.txt!")
        return
    print("🔄 Устанавливаем зависимости...")
    subprocess.check_call([sys.executable, "-m", "pip", "install", "-r", "requirements.txt"])

def resource_path(path):
    return os.path.join(os.path.abspath(os.path.dirname(__file__)), "..", path)

memory = Memory(
    memory_path=resource_path("data/memory.json"),
    knowledge_path=resource_path("data/knowledge_base.json")
)
tokenizer = Tokenizer()
classifier = Classifier()
trainer = Trainer()

HELP = """
🧪 Расширенная Dev Console (команды через /):

  📄 Общие:
    /help                    — помощь
    /exit                    — выход
    /run                     — запустить run.py

  📦 Память:
    /mem facts [субъект]     — показать факты (по желанию: по субъекту)
    /mem knowledge [тег]     — показать знания (по желанию: по тегу)
    /mem export md|csv       — экспорт

  ➕ Изменения:
    /add fact subj pred obj [source] — добавить факт
    /del fact id                     — удалить факт

  🧠 Интеллект:
    /plan                   — цели обучения
    /meta                   — мета-журнал размышлений
    /goals                  — последние цели
    /log                    — последние действия

  🧪 Тест:
    /test фраза — токенизация + классификация
"""

def print_facts(subj=None):
    facts = [f for f in memory.facts if not subj or subj.lower() in f.subject.lower()]
    if not facts:
        print("❌ Факты не найдены.")
        return
    for f in facts:
        print(f"- ID:{f.id} | {f.subject} — {f.predicate} — {f.obj} | from: {f.source or 'n/a'}")

def print_knowledge(tag=None):
    entries = [k for k in memory.knowledge if not tag or tag.lower() in map(str.lower, k.tags)]
    if not entries:
        print("❌ Знания не найдены.")
        return
    for k in entries:
        print(f"- ID:{k.id} | {k.title} [{k.type.value}] — {', '.join(k.tags)}")
        print(f"  > {k.content}")

def export_memory(fmt):
    if fmt == "md":
        with open("memory_report.md", "w", encoding="utf-8") as f:
            f.write("# 🧠 Память ИИ\n\n## 📌 Факты\n\n")
            for fact in memory.facts:
                f.write(f"- **{fact.subject}** — *{fact.predicate}* — {fact.obj}\n")
                f.write(f"  Источник: `{fact.source}`\n  Время: `{fact.timestamp}`\n\n")
            f.write("## 📚 Знания\n\n")
            for k in memory.knowledge:
                f.write(f"- **{k.title}** ({k.type.value})\n  > {k.content}\n")
                f.write(f"  Теги: {', '.join(k.tags)}\n  Время: `{k.created}`\n\n")
        print("✅ Экспорт в memory_report.md")
    elif fmt == "csv":
        with open("memory_report.csv", "w", encoding="utf-8", newline='') as f:
            writer = csv.writer(f)
            writer.writerow(["Type", "ID", "Subject/Title", "Predicate", "Object/Content", "Source/Tags", "Timestamp/Created"])
            for fct in memory.facts:
                writer.writerow(["Fact", fct.id, fct.subject, fct.predicate, fct.obj, fct.source or "", fct.timestamp])
            for k in memory.knowledge:
                writer.writerow(["Knowledge", k.id, k.title, "", k.content, ";".join(k.tags), k.created])
        print("✅ Экспорт в memory_report.csv")
    else:
        print("⚠️ Формат должен быть md или csv.")

def add_fact(args):
    if len(args) < 3:
        print("⚠️ Требуется: subj pred obj [source]")
        return
    subj, pred, *rest = args
    obj = " ".join(rest[:-1]) if len(rest) > 1 else rest[0]
    source = rest[-1] if len(rest) > 1 else None
    memory.add_fact(subj, pred, obj, source)
    print(f"✅ Факт: {subj} — {pred} — {obj}")

def del_fact(args):
    if len(args) != 1:
        print("⚠️ Укажите ID")
        return
    fid = args[0]
    orig = len(memory.facts)
    memory.facts = [f for f in memory.facts if f.id != fid]
    memory._save_facts()
    print("✅ Удалено" if len(memory.facts) < orig else "❌ Не найден")

def handle_test(phrase):
    tokens = tokenizer.tokenize(phrase)
    ptype = classifier.classify(phrase)
    print(f"[Test] Тип: {ptype.name}")
    print(f"[Test] Токены: {tokens}")

def handle_journal_commands(cmd):
    if cmd == "plan":
        print("\n📌 Цели на обучение:")
        for task in trainer.plan_learning():
            print("-", task)

    elif cmd == "meta":
        print("\n🧠 Последние размышления:")
        entries = trainer.journal.get_recent_entries()
        for e in entries:
            try:
                tag = e.split("'tag': '")[1].split("'")[0]
                thought = e.split("'thought': '")[1].rsplit("'", 1)[0]
                print(f"[{tag}] {thought}")
            except:
                print(e.strip())

    elif cmd == "goals":
        print("\n🎯 Последние цели:")
        goals = [e for e in trainer.journal.get_recent_entries(20) if "'goal'" in e or 'Цель' in e]
        for g in goals[-5:]:
            try:
                thought = g.split("'thought': '")[1].rsplit("'", 1)[0]
                print(f"[goal] {thought}")
            except:
                print(g.strip())

    elif cmd == "log":
        print("\n📜 Последние действия:")
        for line in trainer.reporter.get_recent_logs():
            print(line.strip())


def main():
    print(HELP)
    while True:
        try:
            line = input(" > ").strip()
            if not line:
                continue
            if not line.startswith("/"):
                trainer.process_text(line)
                continue

            parts = line[1:].split()
            cmd, args = parts[0].lower(), parts[1:]

            match cmd:
                case "exit": break
                case "help": print(HELP)
                case "run":
                    run_path = resource_path("run.py")
                    if os.path.exists(run_path):
                        subprocess.run([sys.executable, run_path])
                        memory.facts = memory._load_facts()
                        memory.knowledge = memory._load_knowledge()
                    else:
                        print(f"⚠️ run.py не найден: {run_path}")
                case "mem":
                    if not args: print("⚠️ Уточните: facts, knowledge, export"); continue
                    subcmd = args[0].lower()
                    if subcmd == "facts": print_facts(args[1] if len(args) > 1 else None)
                    elif subcmd == "knowledge": print_knowledge(args[1] if len(args) > 1 else None)
                    elif subcmd == "export": export_memory(args[1] if len(args) > 1 else "")
                    else: print("⚠️ Неизвестная команда mem.")
                case "add" if args and args[0] == "fact": add_fact(args[1:])
                case "del" if args and args[0] == "fact": del_fact(args[1:])
                case "test": handle_test(" ".join(args))
                case "plan" | "meta" | "goals" | "log": handle_journal_commands(cmd)
                case _: print("⚠️ Неизвестная команда. /help для списка.")
        except KeyboardInterrupt:
            print("\n🛑 Прерывание.")
            break
        except Exception as e:
            print(f"⚠️ Ошибка: {e}")
            traceback.print_exc()

if __name__ == "__main__":
    main()
